###
# kubernetes proxy config

# default config should be adequate

# Add your own!
KUBE_PROXY_ARGS="--kubeconfig={{ kube_config_dir }}/kube-proxy.kubeconfig --hostname-override={{ inventory_hostname }}"
