. {{ kube_config_dir }}/config
. {{ kube_config_dir }}/kubelet

KUBELET_OPTS="$KUBE_LOGTOSTDERR \
              $KUBE_LOG_LEVEL \
              $KUBELET_API_SERVER \
              $KUBELET_ADDRESS \
              $KUBELET_PORT \
              $KUBELET_HOSTNAME \
              $KUBE_ALLOW_PRIV \
              $KUBE_CLOUD_PROVIDER \
              $KUBELET_ARGS"
