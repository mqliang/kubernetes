{
    "name": "calico-k8s-network",
    "type": "calico",
    "etcd_endpoints": "{{ etcd_endpoints }}",
    "log_level": "info",
    "ipam": {
        "type": "calico-ipam",
        "subnet": "{{ calico_subnet }}/{{ calico_prefix }}"
    }
    {# this block is needed by network policy #}
    {#
    "policy": {
        "type": "k8s",
        "k8s_api_root": "http://$masterIP:8080/api/v1"
    }
    #}
}
