apiVersion: v1
kind: ResourceQuota
metadata:
  name: cyclone
  namespace: cyclone
  labels:
    kubernetes-admin.caicloud.io/type: partition
    kubernetes-admin.caicloud.io/partition: cyclone
spec:
  hard:
    cpu: "8"
    limits.cpu: "8"
    limits.memory: 32Gi
    memory: 32Gi
